{
  "name": "receipt-verification-system",
  "version": "1.0.0",
  "description": "A blockchain-based receipt verification system using Solidity and React",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js --network localhost",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia",
    "start": "cd frontend && npm start",
    "dev": "hardhat node",
    "install-frontend": "cd frontend && npm install"
  },
  "keywords": ["blockchain", "solidity", "receipt", "verification", "ethereum"],
  "author": "Receipt Verification Team",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0",
    "chai": "^4.3.10"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.3.1",
    "ethers": "^6.14.0"
  }
}
